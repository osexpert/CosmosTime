using System;
using System.Collections.Generic;
using System.Text;

namespace CosmosTime
{

	//public struct LocalTime
	//{
	//	DateTime _dt;

	//	// Parse: allow only local without Z or timezone ? Or support converting to local?

	//	public LocalTime(DateTime local)
	//	{
	//		if (local.Kind != DateTimeKind.Local)
	//			throw new Exception("not local time");

	//		_dt = local;
	//	}
	//}

}
